# 02 · Roadmap: the September 23 build

Thin vertical slices, dependency-ordered, each with the check that proves it. This is the self-generated task queue for the ship run; milestones are inserted when building reveals them. Dates assume today is Sep 17.

| # | Phase | Goal | Files | Acceptance and proof | Risk |
| --- | --- | --- | --- | --- | --- |
| M0 | P0 | Workspace, fork, mint read, seed, time travel | `Anchor.toml`, `Cargo.toml`, `programs/roster_finance` stub, `scripts/{fork,anchor-test,idl-sync}.sh`, `scripts/{seed-fork,probe-mint}.ts`, `packages/core`, `tests/e2e/p0-foundation.test.ts`, `docs/PLAN.md` | `pnpm anchor:build` green on the stub; `pnpm test:fork` funds a wallet with NVDAx (ATA created by instruction, then `surfnet_setTokenAccount`) and USDC, reads the effective multiplier from the mint, `transferChecked` between two seeded wallets succeeds, time-travels to a Saturday and reports equity closed / token live; Hermes price assertion skips with a named warning without `PYTH_CORE_API_KEY` | public RPC rate limits; cheatcode account layout |
| M1 | P1a | Program core with the corrected accounting | `programs/roster_finance/src/{state,error,events,math,instructions/*}`, `tests/program.rs`, `tests/common/{mint,clock}.rs` | litesvm suite on a synthetic NVDAx replica (ScaledUiAmount + Pausable + hook slot None): `create_market`, `create_series` mints a Token-2022 position mint with metadata, `quote`, `cancel_ask`, `withdraw_unsold`, `claim_premium`, `buy` (3-ask walk, partial fill, `QuoteMoved`), `exercise` (day 1, mid, 1 s before, after → rejected, double → rejected), `settle_writer`, `close_series`; the four skeptic counterexamples pass; addendum E prime-`sold` dust test; addendum F equal payouts; multiplier-invariance (replica `update_multiplier`, strike unchanged) | the metadata-init CPI ordering (first test on purpose) |
| M2 | P1b | Fees, auto-exercise, halt, invariants, fork run | `instructions/{set_fee_schedule,withdraw_fees,enable_auto_exercise,auto_exercise,pause}`, property test, `tests/e2e/p1-fork-lifecycle.test.ts`, `docs/RENT.md`, `docs/COMPUTE.md` | fee vault balances after a buy; `auto_exercise` with a `PriceUpdateV2` fixture (fresh ITM fires, stale / OTM / wrong feed declines); pause gates only the listed instructions; halt rule; random-sequence invariant test; the same lifecycle on the real NVDAx mint on the fork with `surfnet_timeTravel`; rent and CU measured | `pyth-solana-receiver-sdk` 2.0.0 compile; eviction path |
| M3 | P2 | Services on the fork | `packages/{sdk,oracle,quoter,keeper,indexer,services}`, `tests/e2e/p2-services.test.ts` | one process posts quotes for every grid series of the Tier 1 market, survives a time-travelled weekend (6 h steps, closed-session multiplier), auto-exercises an absent ITM holder at a time-travelled expiry, fills and exercises appear in the indexer, `GET /v1/protection/:series` returns fillable sizes at three notionals with vault addresses | `PYTH_CORE_API_KEY` missing → quoter cannot price; stops with the one-line request |
| M4 | P3 | Web on real data, wallet flow, compliance | `apps/web` tx builders, `/api/tx/send`, `middleware.ts` geo gate, `/risk /terms /privacy /fees`, market list + term grid (quotable vs live), partial-fill line, Manage delegate state, copy FIX list, addendum H landing changes | a person buys a call, buys a put, writes a put, exercises, sees the release in a browser with no terminal (Playwright drives it against the fork); screenshots 1280/390; geo-blocked request gets the plain message | Lenis + wallet modal interplay; Turbopack transpilePackages |
| M5 | P7-lite | Registry and launch set on the fork | `packages/registry`, `scripts/eligibility.ts`, `docs/ELIGIBILITY.md`, `create_market` for Tier 1 (NVDAx, TSLAx, SPYx) and the Tier 2 xStocks that pass | every launch-set market exists on the fork with quotes from the right tier; eligibility report written; Backpack held | `TOKENS_XYZ_API_KEY` missing → registry from the xStocks API only, tokens.xyz tiers deferred |
| M6 | P5 | Mainnet, Sep 21 evening | `scripts/anchor-deploy.sh`, `docs/SEEDING.md`, `docs/UPGRADE.md` | **STOP and ask**: deploy plan, treasury size per market, per-term caps; on approval deploy, transfer upgrade authority to the multisig, seed Tier 1 small, post quotes with a Sep 22 16:00 NY expiry and Friday | needs deployer key, multisig, treasury, keeper and quoter wallets |
| M7 | P4 | Protected Buy, First Print | client-composed Jupiter swap + `buy`; `has_transfer_fee` path; `/buy`, `/pre-ipo` on real data | one Protected Buy round-trips on the fork; one tKalshi contract round-trips with fees reconciled to the unit | `JUPITER_API_KEY` if the tier needs it |
| M8 | P6 | Submission | README "what is live" table, video off the tape, bounty forms | a judge can verify every "live" claim in one click | none |

Cut order if late on Sep 22 (inside a phase, never P6): Tier 2 markets 8 → 3; First Print on-chain contracts; the on-chain Pyth check in `auto_exercise`; Protected Buy; treasury P&L on the roster page. Never the invariant tests, never the mainnet tape.
