# Mint read: launch set and pre-IPO tokens

Read on mainnet at slot 447797526, epoch 1036, 2026-09-17T12:40:21Z, through `https://api.mainnet-beta.solana.com` with `scripts/probe-mints.cjs` (`@solana/web3.js` 1.99.0, `@solana/spl-token` 0.4.15, manual TLV walk cross-checked against the library getters). Raw output: `fixtures/probes/mint_probe_results.json`; per-symbol API responses: `fixtures/probes/xstocks/*.json`, `prestocks.json`, `tessera_token_details.json`. Mints come from the issuers' APIs, never from memory (CLAUDE.md §2.1).

## Table

| Symbol | Mint | Dec | Extensions | Transfer hook | Effective multiplier | Transfer fee | Permanent delegate | Freeze auth | Pausable |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| NVDAx | `Xsc9qvGR1efVDFGLrVsmkzv3qi45LTBjeUKSPmx9qEh` | 8 | MetadataPointer, PermanentDelegate, DefaultAccountState, ScaledUiAmountConfig, PausableConfig, ConfidentialTransferMint, TransferHook, TokenMetadata | slot present, program None | 1.001701196801074 | none | 5aMNNLQJ… | JDq14BWv… | pausable |
| TSLAx | `XsDoVfqeBukxuZHWhdvWHBhgEHjGNst4MLodqsJHzoB` | 8 | MetadataPointer, PermanentDelegate, DefaultAccountState, ScaledUiAmountConfig, PausableConfig, ConfidentialTransferMint, TransferHook, TokenMetadata | slot present, program None | 1 | none | 5aMNNLQJ… | JDq14BWv… | pausable |
| SPYx | `XsoCS1TfEyfFhfvj8EtZ528L3CaKBDBRqRapnBbDF2W` | 8 | MetadataPointer, PermanentDelegate, DefaultAccountState, ScaledUiAmountConfig, PausableConfig, ConfidentialTransferMint, TransferHook, TokenMetadata | slot present, program None | 1.005714560286254 | none | 5aMNNLQJ… | JDq14BWv… | pausable |
| SPYx(CLAUDE.md 2.1) | `XsEH7wWfJJu2ZT3UCFeVfALnVA6CP5ur7Ee11KmzVpL` | 8 | MetadataPointer, PermanentDelegate, DefaultAccountState, ScaledUiAmountConfig, PausableConfig, ConfidentialTransferMint, TransferHook, TokenMetadata | slot present, program None | 10 | none | 5aMNNLQJ… | JDq14BWv… | pausable |
| AAPLx | `XsbEhLAtcf6HdfpFZ5xEMdqW8nfAvcsP5bdudRLJzJp` | 8 | MetadataPointer, PermanentDelegate, DefaultAccountState, ScaledUiAmountConfig, PausableConfig, ConfidentialTransferMint, TransferHook, TokenMetadata | slot present, program None | 1.0032690125398187 | none | 5aMNNLQJ… | JDq14BWv… | pausable |
| MSFTx | `XspzcW1PRtgf6Wj92HCiZdjzKCyFekVD8P5Ueh3dRMX` | 8 | MetadataPointer, PermanentDelegate, DefaultAccountState, ScaledUiAmountConfig, PausableConfig, ConfidentialTransferMint, TransferHook, TokenMetadata | slot present, program None | 1.0059033904787456 | none | 5aMNNLQJ… | JDq14BWv… | pausable |
| GOOGLx | `XsCPL9dNWBMvFtTmwcCA5v3xWPSMEBCszbQdiLLq6aN` | 8 | MetadataPointer, PermanentDelegate, DefaultAccountState, ScaledUiAmountConfig, PausableConfig, ConfidentialTransferMint, TransferHook, TokenMetadata | slot present, program None | 1.0023772500603487 | none | 5aMNNLQJ… | JDq14BWv… | pausable |
| AMZNx | `Xs3eBt7uRfJX8QUs4suhyU8p2M6DoUDrJyWBa8LLZsg` | 8 | MetadataPointer, PermanentDelegate, DefaultAccountState, ScaledUiAmountConfig, PausableConfig, ConfidentialTransferMint, TransferHook, TokenMetadata | slot present, program None | 1 | none | 5aMNNLQJ… | JDq14BWv… | pausable |
| METAx | `Xsa62P5mvPszXL1krVUnU5ar38bBSVcWAB6fmPCo5Zu` | 8 | MetadataPointer, PermanentDelegate, DefaultAccountState, ScaledUiAmountConfig, PausableConfig, ConfidentialTransferMint, TransferHook, TokenMetadata | slot present, program None | 1.002298265651938 | none | 5aMNNLQJ… | JDq14BWv… | pausable |
| CRCLx | `XsueG8BtpquVJX9LVLLEGuViXUungE6WmK5YZ3p3bd1` | 8 | MetadataPointer, PermanentDelegate, DefaultAccountState, ScaledUiAmountConfig, PausableConfig, ConfidentialTransferMint, TransferHook, TokenMetadata | slot present, program None | 1 | none | 5aMNNLQJ… | JDq14BWv… | pausable |
| MSTRx | `XsP7xzNPvEHS1m6qfanPUGjNmdnmsLKEoNAnHjdxxyZ` | 8 | MetadataPointer, PermanentDelegate, DefaultAccountState, ScaledUiAmountConfig, PausableConfig, ConfidentialTransferMint, TransferHook, TokenMetadata | slot present, program None | 1 | none | 5aMNNLQJ… | JDq14BWv… | pausable |
| QQQx | `Xs8S1uUs1zvS2p7iwtsG3b6fkhpvmwz4GYU3gWAmWHZ` | 8 | MetadataPointer, PermanentDelegate, DefaultAccountState, ScaledUiAmountConfig, PausableConfig, ConfidentialTransferMint, TransferHook, TokenMetadata | slot present, program None | 1.0027250296551051 | none | 5aMNNLQJ… | JDq14BWv… | pausable |
| tOpenAI | `oPAiAikWTaFj9RYoRFD35ccfwhnMcB3ThgBZRHSkjTZ` | 9 | TransferFeeConfig, MetadataPointer, TokenMetadata | absent | none | 20 bps | none | 7n2PNcDX… | no |
| tKalshi | `TKLSidmLVt3cqGaaodG8tyRzoANfQwoh67AccjmubeZ` | 9 | TransferFeeConfig, MetadataPointer, TokenMetadata | absent | none | 20 bps | none | 7n2PNcDX… | no |
| tSpaceX | `TSPXcLV76s6V2zDiZQ18kBfcbnjaE2ZzNT3ga2Pd99v` | 9 | TransferFeeConfig, MetadataPointer, TokenMetadata | absent | none | 20 bps | none | 7n2PNcDX… | no |
| OPENAI(PreStocks) | `PreweJYECqtQwBtpxHL171nL2K6umo692gTm7Q3rpgF` | 9 | PermanentDelegate, DefaultAccountState, TransferFeeConfig, ConfidentialTransferMint, 16, TransferHook, ScaledUiAmountConfig, MetadataPointer, PausableConfig, TokenMetadata | slot present, program None | 1.4861347 | 50 bps | WV9PJN7X… | WV9PJN7X… | pausable |
| SPACEX(PreStocks) | `PreANxuXjsy2pvisWWMNB6YaJNzr7681wJJr2rHsfTh` | 9 | PermanentDelegate, DefaultAccountState, TransferFeeConfig, ConfidentialTransferMint, 16, TransferHook, ScaledUiAmountConfig, MetadataPointer, PausableConfig, TokenMetadata | slot present, program None | 5 | 50 bps | WV9PJN7X… | WV9PJN7X… | pausable |

The row `SPYx(CLAUDE.md 2.1)` is `XsEH7w…`, which the brief called SPYx. Its on-chain metadata reads **Netflix xStock / NFLXx** and its multiplier is 10 (split). It is not in the launch set; the brief is corrected in place.

## What each extension means for escrow (CLAUDE.md §2.1 open question, Part 2 §2)

**xStocks (all 11 launch-set mints share one layout):** Token-2022, 8 decimals, 677 to 686 bytes.
- `TransferHook`: extension present, `program_id` = None (all zeros), authority `5aMNNLQJ…`. No `ExtraAccountMetaList` PDA exists or is needed; plain `transfer_checked` through the Token-2022 interface works. The issuer can install a hook later without a new mint, so the registry re-reads this field on every refresh and flips the market to `restricted_wrapper(transfer_hook)` if it ever becomes non-zero; the P8 hook path (`spl-transfer-hook-interface` extra-account resolution) is not on the submission critical path.
- `PermanentDelegate` `5aMNNLQJ…`: the issuer can transfer or burn from any token account, including an escrow PDA's. Contract limitation, disclosed in `/risk` and on every ticket's rights line.
- Freeze authority `JDq14BWv…` and `PausableConfig` (same authority, `paused = false` today): the issuer can freeze the vault ATA or pause all transfers. Handled as a soft failure with a clear message; the series halt rule (`docs/01-architecture.md` §4) keeps `exercise` open through `unhalt + 24 h` so a pause spanning expiry cannot transfer intrinsic value from holder to writer.
- `ScaledUiAmountConfig`: raw balances never change; the effective multiplier is `new_multiplier` once `now ≥ new_multiplier_effective_timestamp`, else `multiplier` (spl-token-2022-interface 2.1.0 `scaled_ui_amount/mod.rs`). On-chain effective values matched `api.xstocks.fi …/multiplier` `currentMultiplier` to full precision for all 11 symbols. No activation is pending on any of them today. The program never reads this field except in `auto_exercise` (when `feed_prices_ui_share` is set): the quoter chooses `strike_usdc_per_lot` off-chain from the live multiplier and passes it to `create_series` as given.
- `DefaultAccountState = Initialized`: new ATAs (including vaults) are not frozen at creation.
- `ConfidentialTransferMint`: present, no auditor, auto-approve off; dormant.
- `MetadataPointer` → the mint; `TokenMetadata` names match the API.
- No `TransferFeeConfig`, no `NonTransferable`, no `InterestBearing`, no `MintCloseAuthority`.

**Verdict (Part 2 §2):** `eligible`, flags `permanent_delegate, freeze_authority, pausable, hook_slot_authority_set, scaled_ui`. Escrow transfer still to be proven on the fork before listing (P0 done-criterion).

**Tessera (tOpenAI, tKalshi, tSpaceX):** Token-2022, 9 decimals. `TransferFeeConfig` 20 bps, `maximum_fee = u64::MAX` (uncapped), fee authority `EXvTtxur…` can change bps with a two-epoch delay; freeze authority `7n2PNcDX…`; `MetadataPointer` + `TokenMetadata`; no hook, delegate, pause or multiplier. Every transfer in and out of escrow loses 0.2 %, withheld in the destination account. Use `transfer_checked_with_fee`, credit the writer with the measured net (`vault.reload()` after the CPI), size positions on net, pay out net of a second 0.2 %, re-read the fee config at open and at settle. **Verdict:** `eligible_with_fee (20 bps)`, flag `freeze_authority`.

**PreStocks (OPENAI, SPACEX):** Token-2022, 9 decimals, 902 bytes. `TransferFeeConfig` raised from 0 to **50 bps at epoch 1032** (about a week before this read), uncapped; `PermanentDelegate`, `PausableConfig` (`paused = false`), `TransferHook` slot present with program None, `ScaledUiAmountConfig` (OPENAI effective 1.4861347, SPACEX 5; no API source, on-chain only), `ConfidentialTransferMint`, `ConfidentialTransferFeeConfig` (type 16, not in the JS enum), `DefaultAccountState = Initialized`. One key `WV9PJN7X…` is mint, freeze, permanent-delegate, pause, hook, scaled-UI, fee-config, withdraw-withheld and metadata authority. **Verdict:** `eligible_with_fee (50 bps)`, flags `permanent_delegate, freeze_authority, pausable, hook_slot_authority_set, scaled_ui, single_issuer_key`; the fee is volatile and the concentration is disclosed. The operator may prefer `restricted_wrapper(single_issuer_key)`; mechanically an escrow PDA can hold and transfer today.

## Issuer API notes

- xStocks Assets API base `https://api.xstocks.fi/api/v2`; `GET /public/assets` paginated (`page`, `pageSize`, `nodes[]`, `page.hasNextPage`); `GET /public/assets/{SYMBOL}` with the ticker plus lowercase `x`; Solana mint at `deployments[].network == "Solana"` → `address`; `…/multiplier?network=Solana` → `{currentMultiplier, newMultiplier, activationDateTime, reason}`; history at `…/multiplier/history?page=0&pageSize=N&network=Solana`. NVDAx's last activation: 2026-09-10T00:30Z, dividend, 1.0009180758490996 → 1.001701196801074.
- Tessera `GET https://rest-api.tessera.pe/v1/public/token-details` returned HTTP 500 five times over about five minutes before a 200; fields `id, name, symbol ("T-OpenAI"), code ("tOpenAI"), sector, mint, markPrice, holders, markValuation`. Retry with backoff; `GET /v1/public/tokens` (`token, latest_supply, name, symbol, uri`) was stable.
- PreStocks `GET https://prestocks.com/api/prestocks`: 8 tokens; fields `name, symbol, description, image, external_url, contract_address, markPrice, markValuation, tokenPrice, impliedValuation, supply`. Today: OPENAI mark 965.56 / token 1033.15; SPACEX mark 154.38 / token 123.22 (CLAUDE.md §2.5's 151.17 / 122.52 were the values on its date; the app reads live).

## Not done here

Second-RPC cross-read; freeze and pause history; Tokens API resolution (needs `TOKENS_XYZ_API_KEY`); Backpack entitlement mints (on HOLD per the regulatory lens until their terms are read); the fork escrow transfer proof (P0).
